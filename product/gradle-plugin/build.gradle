task("publishEC", type: Copy) {
    into "../../maven/com/chaquo/python/gradle/$version"
    from "gradle.jar"
    from "gradle.pom"
    rename("gradle", "gradle-$version")
}
publish.dependsOn(publishEC)
